@{
    Layout="_AdminLayout";
}
@model List<Experience>
    <br>
    <h2>Experience List</h2>
<table class="table table-bordered mr-10 ml-10">
    <tr>
        <th>ID</th>
        <th >Title</th>
        <th >SubTitle</th>
        <th >FirstDate</th>
        <th >LastDate</th>
        <th style="width: 200px;"></th>
    </tr>
    @foreach(var item in  Model) {
        <tr>
            <td>@item.ExperienceId</td>
            <td>@item.Title</td>
            <td>@item.SubTitle</td>
            <td> @(((DateTime)item.FirstDate).ToString("dd-MMM-yyyy"))</td>
            <td> @(((DateTime)item.LastDate).ToString("dd-MMM-yyyy")) </td>  
            <td >
                <div class="d-grid gap-2 d-md-block">
                    <form action="/admin/DeleteExperience" method="POST" style="display:inline">
                                <input type="hidden" name="ExperienceId" value="@item.ExperienceId">
                                <button type="submit" class="btn btn-danger">
                                    <i class="fas fa-trash"></i>Delete</button>
                    </form>
                    <a href="/admin/ExperienceEdit/@item.ExperienceId" class="btn btn-warning">
                    <i class="fas fa-pencil-alt"></i>
                    Edit</a>

                </div>
            </td>
        </tr>
    }
</table>
<br>
 <a href="/admin/ExperienceAdd" class="btn btn-success">Add Experience</a>
